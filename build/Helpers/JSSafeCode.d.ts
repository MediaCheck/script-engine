export declare class JSSafeCode {
    private static checkNode(position, goDir?);
    private static trycatch(block);
    private static blockenize(node, prop);
    private static parseSwitcher(node, propName);
    private static parseNode(node);
    static prepareCode(source: string): string;
}
